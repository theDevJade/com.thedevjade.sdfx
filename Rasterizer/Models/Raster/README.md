Optional Sentis ONNX models for neural raster modes.

1. Copy `.onnx` into this folder or anywhere under `Assets/`.
2. Assign the imported ModelAsset in the compiler.
3. Leave empty to use color-quant segmentation.
